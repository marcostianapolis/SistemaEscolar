<!-- Caixa com fundo branco -->
<div class="box-header well">

  <!-- Legenda -->
  <legend>Aluno</legend>

  <p>
    <b>Nome:</b>
    <%= @materia.nome %>
  </p>

  <p>
    <b>Turma:</b>
    <%= @materia.professor.nome %>
  </p>

</div>

<!-- Botão voltar -->
<%= link_to 'Editar', edit_materia_path(@materia), :class => 'btn' %>
<%= link_to 'Voltar', materias_path, :class => 'btn' %>
