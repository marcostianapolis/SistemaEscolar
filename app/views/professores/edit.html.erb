<!-- Caixa com fundo branco -->
<div class="box-header well">
  <%= render 'form' %>
</div>

<!-- Botão para voltar a tela aluno aluno -->
<%= link_to 'Mostrar', @professor, :class => 'btn' %>
<%= link_to 'Voltar', professores_path, :class => 'btn' %>