<!-- Caixa com fundo branco -->
<div class="box-header well">

  <!-- Legenda -->
  <legend>Turma</legend>

  <p>
    <b>Nome:</b>
    <%= @turma.nome %>
  </p>

</div>

<!-- Botão voltar -->
<%= link_to 'Editar', edit_turma_path, :class => 'btn' %>
<%= link_to 'Voltar', turmas_path, :class => 'btn' %>